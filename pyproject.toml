[tool.ruff]
line-length = 100
target-version = "py39"
extend-exclude = ["web-scrapping", "*.ipynb"]

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
